package book;

//import org.springframework.data.jpa.repository.JpaRepository;
/*
 * Not used currrently. Can be used when we start connecting to DB. 
 */
public interface BookDAO  {/*extends JpaRepository{
	
	/*public List<Book> findAllBooks();*/
}
